import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    borderWidth: 1,
    borderColor: "#dbdde0",
    borderRadius: 10,
    margin: 5,
    padding: 5,
    backgroundColor: '#f0f2f4',
    // flexDirection: 'row'
  },
  header:{
    display: "flex",
    flexDirection: "row",
  },
  userDetails: {
    padding: 8
  }
});
